apiVersion: v1
kind: Secret
metadata:
  name: open-terminal-secrets
  namespace: ai
type: Opaque
stringData:
  # generate with: openssl rand -hex 32
  api-key: "changeme"
  # use a dedicated bot user/token with limited access
  github-token: "***"
